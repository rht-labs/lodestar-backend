apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: omp-backend
  name: omp-backend
spec:
  port:
    targetPort: 8080-tcp
  tls:
    termination: edge
  to:
    kind: Service
    name: omp-backend
    weight: 100
  wildcardPolicy: None
